{
  "name": "@go-lan/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Message types and game snapshots shared by the server and the browser",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch --preserveWatchOutput"
  },
  "dependencies": {
    "@go-lan/rules": "*",
    "zod": "^4.1.12"
  }
}
